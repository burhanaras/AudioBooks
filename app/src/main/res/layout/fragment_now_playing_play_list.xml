<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="20dp"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        tools:context=".presentation.ui.nowplaying.playlist.NowPlayingPlayListFragment">

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvNowPlayingList"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingTop="@dimen/list_item_spacing_half"
            android:paddingBottom="@dimen/list_item_spacing_half"
            tools:context=".presentation.ui.nowplaying.bottomsheet.NowPlayingBottomSheetFragment"
            tools:listitem="@layout/item_play_list_layout" />

</FrameLayout>